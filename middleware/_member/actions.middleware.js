// Ban
const ban = (time, reason, by) => {return;}
const unban = () => {return;}
const isBanned = () => {return;}

// Mute
const mute = (time, reason, by) => {return;}
const unmute = () => {return;}
const isMuted = () => {return;}

// Kick
const kick = (reason, by) => {return;}
const isKicked = () => {return;}

// Warn
const warn = (reason, by) => {return;}
const unwarn = () => {return;}
const isWarned = () => {return;}